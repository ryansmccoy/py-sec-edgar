# py-sec-edgar Environment Configuration
# This file contains default configuration that works out of the box.
# Copy this file to .env and customize for your specific environment.

# ============================================================================
# SEC Data Directory Configuration
# ============================================================================

# Primary data directory for SEC filings (cross-platform)
# Default: ./sec_data (relative to project root) - works out of the box
# SEC_DATA_DIR=./sec_data

# ============================================================================
# SEC API Configuration (REQUIRED)
# ============================================================================

# User Agent (REQUIRED by SEC - must include company name and contact email)
# Format: "Company Name ContactEmail@domain.com"
# Default value works for testing, but you MUST change this for production use
USER_AGENT=py-sec-edgar Python Library contact@example.com

# ============================================================================
# Request Rate Limiting (Conservative defaults for reliability)
# ============================================================================

# Delay between requests in seconds (SEC recommends max 10 requests/second)
# Default: 5.5 seconds (very conservative rate limiting)
REQUEST_DELAY=5.5

# Maximum number of retry attempts for failed requests
MAX_RETRIES=3

# Request timeout in seconds
TIMEOUT=30

# ============================================================================
# Logging Configuration
# ============================================================================

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=WARNING

# Enable debug mode (verbose logging and additional checks)
DEBUG=false

# Custom log file path (optional, workflows use their own log files by default)
# LOG_FILE_PATH=logs/py_sec_edgar.log

# ============================================================================
# Filing Processing Defaults (Conservative for testing)
# ============================================================================

# Default forms to process (comma-separated list)
FORMS_LIST=10-K,10-Q,8-K,DEF 14A,13F-HR,SC 13G,SC 13D

# Default ticker symbols to process (comma-separated list)
DEFAULT_TICKERS=AAPL,MSFT,GOOGL,AMZN,TSLA

# ============================================================================
# Index Processing Date Range (Last 2 years by default)
# ============================================================================

# Start date for index processing (m/d/YYYY format)
# Default: 2 years ago (automatically calculated)
# INDEX_START_DATE=1/1/2023

# End date for index processing (m/d/YYYY format)
# Default: Current date (automatically calculated)
# INDEX_END_DATE=12/31/2025

# ============================================================================
# Background Processing (Defaults work for most use cases)
# ============================================================================

# Enable background task processing
USE_BACKGROUND_TASKS=true

# Redis URL for background task queue (optional)
# REDIS_URL=redis://localhost:6379/0

# Database URL for persistent storage (optional)
# DATABASE_URL=sqlite:///./sec_edgar.db

# ============================================================================
# Application Settings
# ============================================================================

# Application name
APP_NAME=SEC EDGAR Filing API

# ============================================================================
# PRODUCTION CONFIGURATION EXAMPLES
# ============================================================================
# Uncomment and modify these settings for production use:

# Production data directory (absolute path recommended)
# SEC_DATA_DIR=/data/sec_filings

# Production User Agent (REQUIRED - replace with your details)
# USER_AGENT=Acme Financial Analytics team@acme-financial.com

# Faster request rate for production (still respects SEC limits)
# REQUEST_DELAY=0.1

# Production logging
# LOG_LEVEL=INFO
# LOG_FILE_PATH=/var/log/py_sec_edgar/application.log

# Database for production persistence
# DATABASE_URL=postgresql://user:pass@db-server/sec_edgar

# ============================================================================
# DEVELOPMENT CONFIGURATION EXAMPLES
# ============================================================================
# Uncomment these settings for development:

# Development data directory
# SEC_DATA_DIR=./dev_data

# Development User Agent
# USER_AGENT=Development Testing dev@localhost

# Slower rate for development/testing
# REQUEST_DELAY=1.0

# Debug logging for development
# LOG_LEVEL=DEBUG
# DEBUG=true

# Disable background tasks for simpler debugging
# USE_BACKGROUND_TASKS=false
